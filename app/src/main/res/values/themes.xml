<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme using platform default. Actual styling is handled in Compose. -->
    <style name="Theme.FlashcardsCustomStudyCards" parent="android:Theme.DeviceDefault.NoActionBar">
        <!-- Optional: Set status bar color to transparent for edge-to-edge -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
